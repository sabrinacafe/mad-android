fun main(){

    val numero = 7 // Definindo o valor do numero a ser verificado

    if (numero > 0){// Verifica se o numero é maior que zero

    println("O numero é positivo") // Ser verdadeiro vai imprimir isso aqui

    } else if (numero < 0){//Caso contrario varifica se o numero é menor que zero

    println("O numero é negativo") //se verdadeiro imprime esse mensagem

    } else {
        println("O numero é zero") //se nenhuma das alternativas for verdadeira
    }
}